import React from 'react';
import NavBar from '../NavBar/NavBar';
import Slider from '../Slider/Slider';

const Home = () => {
    return (
        <div>
            <NavBar />
            <Slider />
        </div>
    );
};

export default Home;